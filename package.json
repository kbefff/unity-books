{
  "name": "unity-books",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "echo \"BUILD OK\" && exit 1",
    "prebuid": "npm run test",
    "test": "echo \"Error: no test specified\" && exit 1",
    "eject": "react-scripts eject"
  }
}
